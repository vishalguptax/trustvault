version: "3.9"

services:
  api:
    build:
      context: ../
      dockerfile: apps/api/Dockerfile
    ports:
      - "3000:3000"
    env_file: ../.env
    environment:
      NODE_ENV: development
      PORT: "3000"
    volumes:
      - ../apps/api/src:/app/apps/api/src
