# === Monorepo-level configuration ===
# These are shared across apps or required by the monorepo tooling.
# App-specific vars belong in apps/api/.env, apps/mobile/.env, etc.

# Expo monorepo fix — required for pnpm workspaces
EXPO_NO_METRO_WORKSPACE_ROOT=1
