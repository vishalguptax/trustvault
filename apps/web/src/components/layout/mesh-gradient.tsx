/** Ambient mesh gradient background — matches the mobile app's visual style. */
export function MeshGradient() {
  return (
    <div className="mesh-gradient" aria-hidden="true">
      <div className="mesh-gradient-blob-3" />
      <div className="mesh-gradient-blob-4" />
    </div>
  );
}
