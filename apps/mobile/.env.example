# API server URL — set to your backend address
# In dev mode, this is auto-detected from Metro host if not set
# For EAS builds, set via eas.json env or EAS secrets
EXPO_PUBLIC_API_URL=http://localhost:8000
