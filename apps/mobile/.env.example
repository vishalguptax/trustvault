# Backend API URL
# Set to your computer's LAN IP so the phone can reach the server
# Find your IP: run "ipconfig" and look for WiFi adapter IPv4 address
# Update this when you switch WiFi networks
EXPO_PUBLIC_API_URL=http://192.168.x.x:8000
