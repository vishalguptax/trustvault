{
  "name": "trustvault",
  "version": "0.1.0",
  "private": true,
  "description": "Verifiable Credential ecosystem prototype — issuer, wallet, verifier, trust registry",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test --filter=@trustvault/api",
    "test:e2e": "turbo test:e2e --filter=@trustvault/api",
    "lint": "turbo lint",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "@swc/core": "^1.15.21",
    "tsx": "^4.21.0",
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "unplugin-swc": "^1.5.9",
    "vitest": "^1.6.1"
  },
  "packageManager": "pnpm@10.18.2",
  "engines": {
    "node": ">=20.0.0"
  }
}
